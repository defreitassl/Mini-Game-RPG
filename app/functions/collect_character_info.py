from ..classes.character import Character

def collect_character_info(page, category, age, gender, strength, agility, health, 
                           stamina, intelligence,height, body_shape, skin_color, 
                           hair_color, biography, picture_id) -> None:

    category_value = category.value
    age_value = age.value
    gender_value = gender.value
    strength_value = strength.value
    agility_value = agility.value
    health_value = health.value
    stamina_value = stamina.value
    intelligence_value = intelligence.value
    height_value = height.value
    body_shape_value = body_shape.value
    skin_color_value = skin_color.value
    hair_color_value = hair_color.value
    biography_value = biography.value
    picture_id_value = picture_id.value